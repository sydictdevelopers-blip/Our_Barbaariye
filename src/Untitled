import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import tailwindcss from '@tailwindcss/vite';

// https://vite.dev/config/
export default defineConfig({
  base: '/frontend/',          // muhiim: si build-ka loogu raro /frontend/
  server: {
    proxy: {
      '/api': {
        target: 'http://127.0.0.1:3000',  // backend-ka Node/Express
        changeOrigin: true,
      },
    },
  },
  plugins: [
    react(),
    tailwindcss(),
  ],
  optimizeDeps: {},
});